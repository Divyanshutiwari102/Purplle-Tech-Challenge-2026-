__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
env/

# data
data/clips/
data/events/
# Pre-computed YOLO detection JSONs are derived from the licensed clips
# and only useful when paired with them — keep them local.
data/detections/
# real anonymised retailer data: keeps customer PII (names, phone numbers).
# Only the derived sample_pos_transactions.csv (no PII) is committed.
data/pos_transactions/
data/layout/*.xlsx
# v2 raw uploads from the Purplle Resource Center (02-Jun-2026): the inner
# `data/new data/Store 1` and `Store 2` directories ship licensed CCTV mp4s
# that must never be pushed. The cleaned v2 sample CSV lives separately
# under data/new_data/ (underscore) and IS committed.
data/new data/
*.db
*.db-wal
*.db-shm
*.sqlite

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Model weights (downloaded on first run)
*.pt
*.onnx
